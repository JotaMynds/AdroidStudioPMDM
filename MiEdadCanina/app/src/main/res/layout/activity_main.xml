<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/cyan"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <!-- FrameLayout permite apilar vistas una encima de la otra. -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="200dp">

        <!-- 1. GIF de fondo (el bosque) -->
        <pl.droidsonroids.gif.GifImageView
            android:layout_width="match_parent"
            android:layout_height="179dp"
            android:scaleType="centerCrop"
            android:src="@drawable/fondo" />

        <!-- 2. Contenedor para posicionar el perro -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="bottom|center_horizontal"
            android:orientation="vertical">

            <!-- 3. El GIF del perro, ahora dentro del LinearLayout -->
            <!-- Su altura se ajusta para no deformarse -->
            <pl.droidsonroids.gif.GifImageView
                android:id="@+id/gifImageView"
                android:layout_width="wrap_content"
                android:layout_height="142dp"
                android:adjustViewBounds="true"
                android:src="@drawable/perro" />
        </LinearLayout>

    </FrameLayout>

    <!-- SE AÑADE EL TEXTO-->

    <!-- SE AÑADE EL CUADRO DE TEXTO-->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:gravity="center"
        android:padding="15dp"
        android:text="@string/calculadora_canina"
        android:textColor="@color/black"
        android:textSize="30sp" />

    <EditText
        android:id="@+id/urEdad"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp"
        android:background="@android:color/transparent"
        android:clickable="false"
        android:focusable="false"
        android:fontFamily="monospace"
        android:gravity="end"
        android:hint="@string/hint"
        android:inputType="none"
        android:longClickable="false"
        android:maxLength="3"
        android:padding="16dp"
        android:textColor="@color/black"
        android:textColorHint="@color/grey"
        android:textSize="36sp"/>

    <!-- SE AÑADE EL VIEW DE TEXTO-->
    <TextView
        android:id="@+id/result_text"
        android:layout_width="match_parent"
        android:layout_height="81dp"
        android:layout_marginStart="30dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="30dp"
        android:fontFamily="monospace"
        android:gravity="center"
        android:padding="10dp"
        android:textColor="@color/black"
        android:textSize="20sp" />

    <!-- PANEL DE BOTONES -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/cyan"
            android:padding="10dp"
            android:columnCount="3">

            <!-- Fila 1 -->
            <Button android:id="@+id/btn1" style="@style/CalculatorButton" android:text="1" />
            <Button android:id="@+id/btn2" style="@style/CalculatorButton" android:text="2" />
            <Button android:id="@+id/btn3" style="@style/CalculatorButton" android:text="3" />

            <!-- Fila 2 -->
            <Button android:id="@+id/btn4" style="@style/CalculatorButton" android:text="4" />
            <Button android:id="@+id/btn5" style="@style/CalculatorButton" android:text="5" />
            <Button android:id="@+id/btn6" style="@style/CalculatorButton" android:text="6" />

            <!-- Fila 3 -->
            <Button android:id="@+id/btn7" style="@style/CalculatorButton" android:text="7" />
            <Button android:id="@+id/btn8" style="@style/CalculatorButton" android:text="8" />
            <Button android:id="@+id/btn9" style="@style/CalculatorButton" android:text="9" />

            <!-- Fila 4 -->
            <Button android:id="@+id/btnClear" style="@style/CalculatorButtonPrimary" android:text="C" />
            <Button android:id="@+id/btn0" style="@style/CalculatorButton" android:text="0" />
            <Button android:id="@+id/btnDel" style="@style/CalculatorButtonPrimary" android:text="DEL"/>
        </GridLayout>
    </LinearLayout>

    <!-- BOTÓN CALCULAR -->
    <Button
        android:id="@+id/boton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginStart="60dp"
        android:layout_marginEnd="60dp"
        android:layout_marginBottom="7dp"
        android:backgroundTint="@color/lila"
        android:fontFamily="monospace"
        android:padding="14dp"
        android:text="@string/calcular"
        android:textColor="@color/white"
        android:textSize="18sp" />

</LinearLayout>
